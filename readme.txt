The problem statement was to find questions that are related to one another based on a given topic. Since keyword can't be extracted on this dataset using RAKE or TF-IDF algorithms(These algorithms use frequency as a basis of classification and this datset contains small sentences with little to no redundancy in appearence), Bag of Words and Similar String matching has been used. It is an application of NLTK library of Python for NLP(Natural Language Processing).